/* Generated by the STM32 engineering assistant.
 *
 * The clock tree, the pins and the peripheral setup below were generated
 * from the design plan. Application code belongs between the USER CODE
 * markers: everything outside them is rewritten when the project is
 * regenerated, including after a failed build.
 */
#include "main.h"

/* USER CODE BEGIN Includes */

/* USER CODE END Includes */

{{HANDLES}}
/* USER CODE BEGIN PV */

/* USER CODE END PV */

void SystemClock_Config(void);
static void MX_GPIO_Init(void);
{{PROTOTYPES}}
/* USER CODE BEGIN PFP */

/* USER CODE END PFP */

int main(void)
{
  /* USER CODE BEGIN 1 */

  /* USER CODE END 1 */

  HAL_Init();
  SystemClock_Config();

  /* USER CODE BEGIN SysInit */

  /* USER CODE END SysInit */

  MX_GPIO_Init();
{{INIT_CALLS}}
  /* USER CODE BEGIN 2 */

  /* USER CODE END 2 */

  while (1)
  {
    /* USER CODE BEGIN WHILE */

    /* USER CODE END WHILE */
  }
}

{{CLOCK_CONFIG}}
{{GPIO_INIT}}
{{INIT_FUNCTIONS}}
/* USER CODE BEGIN 4 */

/* USER CODE END 4 */

/* Reached when a HAL call fails. Left as a trap on purpose: a silent
 * Error_Handler turns a broken clock configuration into a board that looks
 * alive and does nothing. */
void Error_Handler(void)
{
  __disable_irq();
  while (1)
  {
  }
}

#ifdef USE_FULL_ASSERT
void assert_failed(uint8_t *file, uint32_t line)
{
  /* USER CODE BEGIN 6 */

  /* USER CODE END 6 */
}
#endif
