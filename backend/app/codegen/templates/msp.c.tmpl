/* Low-level peripheral setup, generated by the STM32 engineering assistant.
 *
 * The HAL calls these while initialising a peripheral. Everything a
 * peripheral needs from the rest of the chip -- its clock, its pins, its
 * interrupt -- is here rather than in main.c, which is the split CubeMX
 * uses and the reason a peripheral can be reconfigured without touching
 * application code.
 */
#include "main.h"

{{MSP_EXTERNS}}
void HAL_MspInit(void)
{
  __HAL_RCC_SYSCFG_CLK_ENABLE();
  __HAL_RCC_PWR_CLK_ENABLE();

  /* USER CODE BEGIN MspInit */

  /* USER CODE END MspInit */
}

{{MSP_FUNCTIONS}}
/* USER CODE BEGIN 1 */

/* USER CODE END 1 */
